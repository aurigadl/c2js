#include  <stdio.h>
main()
{
  float suma, sumando;
  suma = 0.00;
  while( scanf ( "%f", &sumando ) == 1  ) {
    suma = suma + sumando;
    printf( "%.2f ", suma );
    printf( "%.3f ", 123.111222 );
    printf( "%s" scanf( "%f", &sumando ));
  }
}
